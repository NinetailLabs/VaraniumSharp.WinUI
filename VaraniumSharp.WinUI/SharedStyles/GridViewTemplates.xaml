<ResourceDictionary
    x:Class="VaraniumSharp.WinUI.SharedStyles.GridViewTemplates"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sortModule="using:VaraniumSharp.WinUI.SortModule">

    <DataTemplate x:Key="SortEntry" x:DataType="sortModule:SortOrderEntry">
        <Border
            BorderBrush="{ThemeResource ToggleSwitchOuterBorderBorderThemeBrush}"
            BorderThickness="1"
            CornerRadius="3"
            Height="38"
            Width="250">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    HorizontalTextAlignment="Left"
                    Margin="5,0"
                    Text="{x:Bind Path=SortHeader}"
                    ToolTipService.ToolTip="{x:Bind Path=SortTooltip}"
                    VerticalAlignment="Center"/>

                <Button
                    Content="{x:Bind Path=SortIcon, Mode=OneWay}"
                    Click="{x:Bind Path=ChangeDirectionClick}"
                    Grid.Column="1"
                    Height="38"
                    HorizontalAlignment="Right"
                    Width="60"/>

            </Grid>
        </Border>
    </DataTemplate>

</ResourceDictionary>
